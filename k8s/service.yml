apiVersion: v1
kind: Service
metadata:
  name: github-actions-demo
  labels:
    app: github-actions-demo
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: github-actions-demo
