apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: sample-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: sample-app
